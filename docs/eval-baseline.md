# Bounty Eval 基线（阶段 0 交付）

> 生成日期：2026-08-13 | 任务集：`scripts/eval/tasks.json`（49 题：A10/B10/C10/D3/E7/F6/G3）
> 判定规则：A=关键点命中；B=测试命令通过+禁改文件未动；C=测试通过+diff 行数≤预算；全部限 max_steps=50

## 结果

| 模型 | pass@1 | A 仓库理解 | B 多文件改动 | C 修 bug | 平均步数 | 平均输入 tok | 平均输出 tok | 工具失败率 | 超时 |
|------|--------|-----------|-------------|---------|---------|-------------|-------------|-----------|------|
| qwen/qwen3.8-max 基线（阶段 0） | **96.7% (29/30)** | 100% (10/10) | 90% (9/10) | 100% (10/10) | 5.6 | 21,031 | 1,303 | 7.3% | 0 |
| qwen/qwen3.8-max P5 全量（20260813-165619） | **100% (49/49)** | 100% (10/10) | 100% (10/10) | 100% (10/10) | 4.1 | 19,817 | 940 | 12.4% | 0 |
| deepseek/deepseek-v4-pro | 待测（需要有效 `DEEPSEEK_API_KEY`，当前环境 key 无效） | - | - | - | - | - | - | - | - |

详细逐题数据：`docs/eval/20260813-165619-report.md`；历史曲线：`docs/eval/history.csv`。

## P5 全量跑分（49/49，2026-08-13）

- **pass@1：96.7% → 100%（49/49）**，A/B/C/D/E/F/G 七类全过；含 D 记忆 3/3、E 生态（MCP/子代理）7/7、F DeVET 攻防 6/6（必需工具 devet_simulate_attack 实际调用）、G 截图报错 3/3。
- **步数**：平均 5.6 → 4.1（↓27%）；A 类 4.4 → 3.1（↓29.5%，验收线 ↓30% 差 0.5pp 未达）。
- **token/任务**：21,031 → 19,817（↓5.8%，验收线 ↓20% 未达）；A 类 ↓4.8%，但 B 类 25,494→29,867（↑17%）、C 类 21,966→27,051（↑23%）——新上下文特性（Repo Map 注入、技能索引+触发注入、自动记忆、todo 摘要）每轮携带成本更高，B/C 类多工具任务放大明显。
- **工具失败率**：7.3% → 12.4%（恶化）——模型在注入更多工具（devet/子代理/MCP）后更频繁调用，失败绝对值小但占比升；B/C 类 edit/写文件失败为主，待锚点补丁+错误反馈整形后续回归验证。
- 结论：**正确率与效率达标，token 与失败率未达验收线**——如实记录，后续阶段（P2 系）回归目标：token/任务 ↓20% 与工具失败率 <5%。

## qwen 失败题分析

- **B9（实现 median）**：代码产物正确（判定器产物自检=测试通过），但模型把首个版本写错目录后，
  用 `del`/`rm`/`node unlink` 清理误放文件，三种命令均被权限门 deny（fail-closed），
  连试 3 次触发"3 连卡死护栏"退出（exit=1）。判定按异常退出计失败。
  → 对应路线图 S12/P2-4（Windows 命令体验）与"权限拦截可解释"：deny 应一次性说明
  "此命令被权限门拦截且无法清理，请忽略误放文件继续"，避免模型反复换命令试探。

## 复现

```powershell
python scripts/eval/selfcheck.py                                   # 49/49 离线自检
python scripts/eval/runner.py --models qwen/qwen3.8-max            # 跑全量 49 题
python scripts/eval/judge.py --run scripts/eval/work/<run_id>      # 判定
python scripts/eval/report.py --run scripts/eval/work/<run_id>     # 报告
```

## 对比口径（8 周目标）

- 目标：pass@1 达基线 1.5–2 倍且 ≥50%；P5 实测 **100%**（达标）；token/任务 ↓40% 未达（当前 ↓5.8%）；工具失败率 <5% 未达（当前 12.4%）。
- 基线已固定（阶段 0 数字），后续阶段每轮回归同一任务集，P2 系（锚点补丁/JSON 修复）与上下文裁剪是下两个杠杆。
