# ts-util

TypeScript 工具函数库（零依赖，Node >= 22.6）。

运行全部测试：

    npm run test

模块：

- `src/strings.ts` — truncate / camelCase / countWords
- `src/numbers.ts` — clamp / formatPercent / sum
- `src/dates.ts` — formatDate / daysBetween
